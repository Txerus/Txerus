name: Build PlayTorrio CarPlay V5 IPA

on:
  push:
    branches:
      - main
    paths:
      - 'ci_patch/**'
      - '.github/workflows/playtorrio-ios.yml'
  workflow_dispatch:

jobs:
  ios:
    runs-on: macos-latest
    timeout-minutes: 90
    steps:
      - name: Checkout workspace
        uses: actions/checkout@v4

      - name: Clone PlayTorrio
        run: git clone --depth 1 https://github.com/ayman708-UX/PlayTorrioV3.git playtorrio

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.41.9'
          channel: stable
          cache: true

      - name: Apply PlayTorrio patches
        run: |
          cp ci_patch/AppDelegate.swift playtorrio/ios/Runner/AppDelegate.swift
          cp ci_patch/ios_native_player.dart playtorrio/lib/services/player/ios_native_player.dart
          cp ci_patch/ios_airplay_hls.dart playtorrio/lib/services/player/ios_airplay_hls.dart
          python3 ci_patch/apply_patch.py
          python3 ci_patch/carplay_v5.py

      - name: Install dependencies
        working-directory: playtorrio
        run: |
          touch .env
          flutter pub get

      - name: Build iOS V5
        working-directory: playtorrio
        run: flutter build ios --release --no-codesign --dart-define-from-file=.env --build-name=1.1.0-v5 --build-number=15

      - name: Verify build
        working-directory: playtorrio
        shell: bash
        run: |
          test -d build/ios/iphoneos/Runner.app
          /usr/libexec/PlistBuddy -c 'Print :UIApplicationSceneManifest' build/ios/iphoneos/Runner.app/Info.plist
          /usr/libexec/PlistBuddy -c 'Print :NSUserActivityTypes' build/ios/iphoneos/Runner.app/Info.plist || true
          find build/ios/iphoneos/Runner.app -iname '*ffmpeg*' -o -iname '*avcodec*' | head -20

      - name: Package V5 IPA
        working-directory: playtorrio
        run: |
          cd build/ios/iphoneos
          rm -rf Payload
          mkdir Payload
          cp -R Runner.app Payload/
          zip -qry ../../../PlayTorrio-AirPlay-PiP-IPTV-CarPlay-V5.ipa Payload

      - name: Upload V5 IPA artifact
        uses: actions/upload-artifact@v4
        with:
          name: PlayTorrio-AirPlay-PiP-IPTV-CarPlay-V5
          path: playtorrio/PlayTorrio-AirPlay-PiP-IPTV-CarPlay-V5.ipa
          if-no-files-found: error
          retention-days: 7
